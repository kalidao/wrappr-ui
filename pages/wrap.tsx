import type { NextPage } from 'next'
import Layout from '../src/layout'

const Wrap: NextPage = () => {
  return <Layout heading="Wrap" content="Wrap any account" back={true}></Layout>
}

export default Wrap
